# AKSCHOLAR — Dépendances Python pour le scraper
# Installer avec : pip install -r requirements.txt

requests==2.31.0
beautifulsoup4==4.12.3
lxml==5.1.0          # Parser HTML rapide pour BeautifulSoup
firebase-admin==6.4.0
schedule==1.2.1
python-dotenv==1.0.0  # Charge les variables depuis .env
